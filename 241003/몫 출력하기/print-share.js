const fs = require("fs");
const input = fs.readFileSync(0).toString().trim().split("\n");

while (true) {
    let idx = 0;
    idx++

    
    if (input[idx] % 2 === 0) {
        console.log(input[idx] / 2);
        break;
        }
    
}